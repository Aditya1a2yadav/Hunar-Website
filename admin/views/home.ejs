<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>All Modules</title>
    <link rel="stylesheet" href="page1.css">
</head>
<body>
    Hello <%= name %>
    <form action="/logout?_method=DELETE" method="post"><button type="submit">Logout</button></form>
    <% if (modules.length === 0) { %>
        <p>No modules found.</p>
    <% } else { %>
        <% for(mod of modules) {%>
            <div class="mod">
                <h4 class="content"><form action="/mod/<%= mod.mod_id %>" method="GET"> <button type="submit"><%= mod.module %></button> </form> </h4>
                <form method="post" action="/mod/<%= mod.mod_id %>/delete?_method=DELETE" onsubmit="return confirmDelete()">
                    <button>Delete Module</button>
                  </form>
                  
                  
                  
                <hr>
            </div>
            <% } %>
    <% } %>
        <br>
        <form action="/new_mod" method="get">
            <button type="submit">Create new Module</button>
         </form>
         <script>
            function confirmDelete() {
              return confirm("Are you sure you want to delete this module? This action cannot be undone.");
            }
          </script>
        <script src="../public/js/delete_confirmation.js"></script>
        <script src="../public/js/name_check.js"></script>
</body>
</html>



